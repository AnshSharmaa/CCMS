import React from "react"
import ReactDom from "react-dom"
import "./index.css"
import App from "./App"

ReactDom.render(<App />, document.getElementById("root"))

// user auth for delete
// user auth for edit(alert: not signed it)
// post placement rip
